<?xml version="1.0" encoding="utf-8"?>
<ScrollView
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="fill_parent"
  android:layout_height="fill_parent"
  android:id="@+id/DonateScrollView">
  <LinearLayout
    android:id="@+id/DonateLinearLayout"
    android:layout_height="fill_parent"
    android:layout_width="fill_parent"
    android:orientation="vertical"
    android:padding="5dp">
    <TextView
      android:textColor="?android:attr/textColorPrimaryDisableOnly"
      android:textAppearance="?android:attr/textAppearanceSmall"
      android:autoLink="all"
      android:id="@+id/DonateTextView"
      android:layout_width="fill_parent"
      android:layout_height="wrap_content"
      android:text="@string/pref_donate_text"
      android:paddingLeft="5dp"
      android:paddingRight="5dp"
      android:paddingTop="5dp"
      android:paddingBottom="12dp" />
    <Button
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:id="@+id/DonateMarketButton"
      android:layout_gravity="center_horizontal"
      android:text="@string/pref_donate_market_button"></Button>
    <Button
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:id="@+id/DonatePaypalButton"
      android:layout_gravity="center_horizontal"
      android:text="@string/pref_donate_paypal_button"></Button>
  </LinearLayout>
</ScrollView>